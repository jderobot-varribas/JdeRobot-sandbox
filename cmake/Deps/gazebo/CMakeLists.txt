
PKG_CHECK_MODULES(GAZEBO QUIET gazebo<=5.1.0)

if (GAZEBO_FOUND)
	MESSAGE(STATUS "*** GAZEBO LIBRARIES FOUND: ${GAZEBO_INCLUDE_DIRS} - ${OGRE_INCLUDE_DIRS}")
else()
	MESSAGE(STATUS "*** GAZEBO LIBRARIES NOT FOUND")
endif()

if(GAZEBO_FOUND AND INJECT_DEPS_LIBS)
    include_directories(${GAZEBO_INCLUDE_DIRS})
    link_directories(${GAZEBO_LIBRARY_DIRS})
endif()
